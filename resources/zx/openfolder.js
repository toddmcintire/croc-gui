#!/usr/bin/env zx
/* script to open folder based on specific os type */
import { $ } from `../../node_modules/zx/zx.mjs`;

function openFolder() {
    //let response = await $`ls`;
    document.getElementById("p1").innerHTML = `test`;
};